version: "3.9"

services:
  caddy:
    image: caddy:2
    container_name: caddy
    restart: unless-stopped
    # Use host networking to bypass the massive CPU overhead of docker-proxy
    network_mode: "host"

    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config

  bun:
    build: .
    container_name: streamdrop
    restart: unless-stopped
    # Use host networking so Caddy can reach it via localhost:3000
    network_mode: "host"

    environment:
      - TURN_SERVER=${TURN_SERVER:-}
      - TURN_SECRET=${TURN_SECRET:-}
      - MAX_RECEIVERS=${MAX_RECEIVERS:-2000}

  coturn:
    image: coturn/coturn
    container_name: coturn
    restart: unless-stopped
    network_mode: "host"

    command: >
      -n
      --listening-port=3478
      --listening-ip=0.0.0.0
      --fingerprint
      --lt-cred-mech
      --realm=${TURN_REALM:-streamdrop}
      --static-auth-secret=${TURN_SECRET:-}
      --total-quota=${MAX_RECEIVERS:-2000}
      --user-quota=${TURN_USER_QUOTA:-50}
      --max-bps=${TURN_MAX_BPS:-100000000}
      --stale-nonce=600
      --min-port=49152
      --max-port=65535
      --verbose

volumes:
  caddy_data:
  caddy_config:
